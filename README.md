# Messenger Backend

Backend сервер для мессенджера на Flutter, построенный на Node.js, Express, PostgreSQL и Socket.io.

## 🚀 Возможности

- ✅ Регистрация и аутентификация пользователей (JWT)
- ✅ Создание приватных и групповых чатов
- ✅ Отправка текстовых сообщений
- ✅ Отправка файлов и медиа
- ✅ Real-time сообщения через WebSocket (Socket.io)
- ✅ Поиск пользователей
- ✅ Статусы пользователей (online/offline)
- ✅ Индикатор печати (typing indicator)
- ✅ Отметка сообщений как прочитанных

## 📋 Требования

- Node.js (v18 или выше)
- PostgreSQL (v12 или выше)
- npm или yarn

## 🛠️ Установка

1. **Клонируйте репозиторий и установите зависимости:**

```bash
npm install
```

2. **Создайте базу данных PostgreSQL:**

```sql
CREATE DATABASE messenger_db;
```

3. **Настройте переменные окружения:**

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите свои настройки:

```env
PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=messenger_db
DB_USER=postgres
DB_PASSWORD=2761212Qq

JWT_SECRET=6WcphIJiPfHVCcN6bIPKBZ7RDADTWgmY
JWT_EXPIRES_IN=7d

UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
```

4. **Запустите миграции для создания таблиц:**

```bash
npm run migrate
```

5. **Создайте папку для загрузок:**

```bash
mkdir uploads
```

## 🚀 Запуск

**Режим разработки (с автоперезагрузкой):**

```bash
npm run dev
```

**Продакшн режим:**

```bash
npm start
```

**Запуск через PM2 (рекомендуется для продакшн):**

```bash
npm run pm2:start
```

Сервер запустится на `http://localhost:3000`

## 🌐 Деплой на VPS сервер

Подробная инструкция по развертыванию на VPS сервере находится в файле **[DEPLOY.md](./DEPLOY.md)**

Краткая версия:
1. Установите Node.js, PostgreSQL, PM2 и Nginx на сервере
2. Загрузите проект на сервер (через Git или SCP)
3. Настройте `.env` файл с продакшн настройками
4. Запустите миграции: `npm run migrate`
5. Запустите через PM2: `pm2 start ecosystem.config.js --env production`
6. Настройте Nginx как reverse proxy (см. `nginx.conf.example`)

## 📡 API Endpoints

### Аутентификация

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/verify` - Проверка токена

### Пользователи

- `GET /api/users/me` - Получить информацию о текущем пользователе
- `GET /api/users/search?q=query` - Поиск пользователей
- `GET /api/users/:id` - Получить пользователя по ID

### Чаты

- `POST /api/chats` - Создать новый чат
- `GET /api/chats` - Получить все чаты пользователя
- `GET /api/chats/:id` - Получить чат по ID
- `PATCH /api/chats/:id` - Обновить чат
- `POST /api/chats/:id/participants` - Добавить участника
- `DELETE /api/chats/:id/participants/:userId` - Удалить участника

### Сообщения

- `GET /api/messages/chat/:chatId` - Получить сообщения чата
- `POST /api/messages` - Создать новое сообщение
- `POST /api/messages/chat/:chatId/read` - Пометить сообщения как прочитанные
- `GET /api/messages/:id` - Получить сообщение по ID

### Загрузка файлов

- `POST /api/upload` - Загрузить один файл
- `POST /api/upload/multiple` - Загрузить несколько файлов

## 🔌 WebSocket Events (Socket.io)

### Клиент → Сервер

- `message:send` - Отправить сообщение
- `messages:read` - Пометить сообщения как прочитанные
- `typing:start` - Начало печати
- `typing:stop` - Конец печати
- `chat:join` - Присоединиться к чату

### Сервер → Клиент

- `message:new` - Новое сообщение
- `messages:read` - Сообщения помечены как прочитанные
- `typing:start` - Пользователь начал печатать
- `typing:stop` - Пользователь закончил печатать
- `user:status` - Изменение статуса пользователя
- `chat:updated` - Обновление чата
- `chat:joined` - Успешное присоединение к чату
- `error` - Ошибка

## 📝 Примеры запросов

### Регистрация

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "password": "password123"
  }'
```

### Вход

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "john@example.com",
    "password": "password123"
  }'
```

### Создание чата

```bash
curl -X POST http://localhost:3000/api/chats \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "type": "private",
    "participantIds": [2]
  }'
```

### Отправка сообщения

```bash
curl -X POST http://localhost:3000/api/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "chatId": 1,
    "content": "Привет! Как дела?",
    "messageType": "text"
  }'
```

## 🔐 Аутентификация

Все защищенные endpoints требуют JWT токен в заголовке:

```
Authorization: Bearer YOUR_JWT_TOKEN
```

Для WebSocket подключения передайте токен в параметре `auth.token`:

```javascript
const socket = io('http://localhost:3000', {
  auth: {
    token: 'YOUR_JWT_TOKEN'
  }
});
```

## 📁 Структура проекта

```
.
├── config/
│   └── database.js          # Конфигурация PostgreSQL
├── middleware/
│   └── auth.js              # JWT аутентификация
├── models/
│   ├── User.js              # Модель пользователя
│   ├── Chat.js              # Модель чата
│   └── Message.js           # Модель сообщения
├── routes/
│   ├── auth.js              # Роуты аутентификации
│   ├── users.js             # Роуты пользователей
│   ├── chats.js             # Роуты чатов
│   └── messages.js          # Роуты сообщений
├── socket/
│   └── socketHandler.js     # Обработчики Socket.io
├── scripts/
│   └── migrate.js           # Миграции базы данных
├── uploads/                  # Папка для загрузок
├── server.js                 # Главный файл сервера
├── package.json
└── README.md
```

## 🐛 Отладка

Для просмотра логов в консоли включите `NODE_ENV=development` в `.env`.

## 📝 Лицензия

ISC

## 🤝 Вклад

Приветствуются любые улучшения и предложения!

